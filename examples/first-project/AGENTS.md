# Project Agent Instructions

> Clear paths guide the work
> State remains where agents meet
> Truth survives each handoff

This project uses U-GAS.

Canonical U-GAS authority:
https://github.com/jaabster-dev/u-gas

For a fresh session, start from this local `AGENTS.md`, read the current upstream U-GAS `AGENTS.md` and follow its routing, then inspect this example repository's current PICA files and actual project state. Project-specific rules may extend U-GAS when they are real; do not invent product meaning. If upstream cannot actually be read, report that capability boundary instead of guessing current U-GAS rules.
